# コンテンツ・設定TODO

旧サイト `aocli.com`（医療法人 青山整形外科クリニック公式サイト）を解析し、実データを移行して
このNext.js版サイトを構築しました。以下は実データ化済みの項目と、クリニック側での確認・
入力が必要な残項目のまとめです。

## 実データに差し替え済み

- 住所・電話番号・FAX・メールアドレス（`src/lib/clinic-info.ts`）
- 診療時間・受付時間・休診日（`/hours`）
- 院長・スタッフ紹介、整形外科専門医についての解説（`/doctor`）
- 診療案内（一般整形外科・スポーツ整形外科・リハビリテーション科・スポーツ障害予防）（`/medical`）
- 医院全景・院内紹介の写真（旧サイトの実写真を取得し `public/images/` に配置、alt属性も旧サイトのキャプションを使用）
- プライバシーポリシー全文（`/privacy`）
- ダウンロード資料（スポーツ部活動帯同依頼書・スポーツヘルスチェックシート・メディカルチェックシート）の実ファイル（`public/downloads/`）
- LINKS・院長の談話室・スタッフ日記（`/links`, `/column`）
- 地図（旧サイトのYahoo!地図APIに設定されていた緯度経度をGoogleマップ埋め込みに置き換え）

## クリニック側で確認・入力が必要な項目

- **採用情報（`/recruit`）**：旧サイトに該当ページがなく、募集要項の実データがありません。
  現在は「準備中」表示のプレースホルダーです。募集職種・条件が決まり次第、
  `src/app/recruit/page.tsx` を更新してください。
- **FAQ（`/faq`）の一部回答**：旧サイトのQ&Aページは空でした。以下はサイト内の他ページの
  事実から作成した安全側の回答で、要確認です。
  - 「クレジットカードは使えますか？」→ 現金・保険証持参としており、カード対応は未確認。
  - 「労災保険は対応していますか？」→ 未確認のため受付へ確認を促す文言のみ。
  - 「紹介状は必要ですか？」→ 診療所は紹介状不要が一般的という前提で記載。実際の運用を確認してください。
- **スタッフ人数**：旧サイトのページ間で人数表記に差異がありました（院長紹介ページでは
  理学療法士3名、リハビリ室ページでは4名との記載）。`/doctor` には注記を入れていますが、
  正確な最新人数に更新してください。
- **お知らせ（`/news`）の過去データ**：初期シード（`prisma/seed.ts`）には旧サイトの過去の
  休診案内（2021〜2022年）をアーカイブとして投入しています。実運用開始後は
  `/admin/news` から最新のお知らせを追加してください。

## 公開前に必ず設定する項目（技術面）

- `.env` の `ADMIN_PASSWORD` / `SESSION_SECRET` を推測困難な値に変更する。
- 本番用データベース（Vercel Postgres / Supabase 等）を用意し `DATABASE_URL` を差し替える
  （SQLiteのままサーバーレス環境にデプロイすると、書き込んだデータが永続化されません）。
  `prisma/schema.prisma` の `datasource.provider` も `postgresql` 等に変更が必要です。
- `NEXT_PUBLIC_SITE_URL` に本番ドメインを設定する（`sitemap.xml`・OGP・canonicalに反映されます）。
- `robots.ts` / 各ページの `metadata.robots` は公開用に `index, follow` 設定済みです
  （`/admin` 配下のみ `noindex` を明示）。

## 今回スコープ外（将来拡張として設計のみ考慮）

Web問診／LINE通知／待ち人数表示／AIチャットボット／スマホ診察券／リハビリ動画／
処方箋案内／オンライン診療／患者マイページ。DBスキーマはシンプルな構成にしてあり、
今後モデル・フィールドを追加しやすい設計です。
