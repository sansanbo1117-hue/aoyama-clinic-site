# 受付ワークベンチ・予約セルフサービス再設計 実装計画書

## 1. 目的

この改修の目的は、機能を増やすことではなく、患者と受付スタッフが自力で完了できる仕事を増やすことである。

### 患者側

- メールが見つからなくても予約を確認できる
- 電話をせずに予約日時を変更・取消できる
- 予約番号を持っていれば、サイト上の常設入口から手続きを開始できる

### 受付側

- 今日の時間別の予約数・定員・残数を一目で判断できる
- 電話予約を空いている時間枠へ直接登録できる
- 予約なし患者を受け入れられるか、その場で判断できる
- 通常業務ではなく例外だけが「要対応」として表示される

## 2. 第一原理から定義する受付画面の役割

受付スタッフが画面を開く理由は、主に次の3点である。

1. 今日、何時に誰が来るか確認する
2. 各時間にあと何人受け入れられるか判断する
3. 通常処理できなかった例外だけを解決する

したがって、受付ホームの情報優先順位を次の順番へ変更する。

1. 今日の日付と診療状況
2. 時間別の予約枠・定員・残数
3. 各患者の受付状態
4. 電話予約・窓口受付のクイック操作
5. 未解決の例外（存在するときだけ）
6. コンテンツ管理・通知設定などの低頻度業務

## 3. 現状の問題

### 3.1 患者の予約確認

- 予約確認ページに常設入口はあるが、実際の確認にはメール内トークンが必要
- メールが見つからない患者を電話へ誘導している
- Web予約で減らすべき受付業務が再発生する
- URLトークン方式だけでは、別端末での再確認が難しい

### 3.2 受付ホーム

- 即時確定型の `Appointment` と、旧来の依頼型 `Reservation` が混在している
- 「未対応の予約依頼」「対応中の予約」は即時確定型では不要
- 「確定済みWeb予約」の総数は、今日の受付判断に直接役立たない
- 0件のカードや空の「要対応」が画面面積を使っている
- 予約患者一覧はあるが、時間枠の定員と残数が見えない

### 3.3 ナビゲーション

- 日常の受付操作と低頻度の設定・コンテンツ管理が同列に並んでいる
- タブレット上で目的の機能を探す負担が大きい
- 「電話予約」「診療枠管理」は受付ホームと分離する必要が薄い

### 3.4 配色

- 濃い緑が見出し、リンク、ボタン、ナビゲーションへ広く使われている
- 色が操作の優先順位を伝えていない
- 大きな濃色面が多く、受付業務画面として視覚的な疲労が出やすい

### 3.5 Preview環境のCSP警告

- Vercel Preview Toolbarの `https://vercel.live` スクリプトが本番向けCSPにより拒否されている
- サイト本体の予約処理には影響しないが、デモ時のコンソールに不要な警告が残る

## 4. 改修後の情報設計

## 4.1 患者向け入口

公開サイトのヘッダー、モバイルメニュー、Web予約ページに、常に次の2操作を分けて表示する。

- 新しく予約する
- 予約の確認・変更・取消

「予約の確認・変更・取消」を開くと、メール案内ではなく本人確認フォームを最初に表示する。

### 本人確認項目

- 予約番号
- 予約時の電話番号
- 生年月日

生年月日が未登録の古い予約だけは、予約番号・電話番号・氏名カナなどの代替照合を検討する。新規予約では生年月日を必須に統一する。

### 照合成功後

- 予約日時
- 初診／再診
- 予約状態
- 予約番号
- 日時変更ボタン
- 取消ボタン
- 新しい予約ボタン

を表示する。

### 照合失敗時

「どの項目が間違っているか」は表示せず、常に同じエラー文を返す。

> 入力内容と一致する予約を確認できませんでした。

これにより、予約番号や患者情報の存在確認に悪用されることを防ぐ。

## 4.2 予約管理セッション

照合成功後は、予約情報をそのままレスポンスへ返さず、短時間の予約管理セッションを発行する。

- HttpOnly
- Secure
- SameSite=Lax
- 有効期限15分
- 対象予約IDを署名
- `/appointments/manage` に用途を限定

既存のメール内トークンは廃止せず、開いたときに同じ短時間セッションへ交換する。これにより既存メールとの互換性を維持する。

URLへ長期間有効なトークンを残し続ける方式は段階的に縮小する。

## 4.3 受付ホーム

受付ホームを「今日のタイムライン」に一本化する。

### 画面上部

- 今日の日付・曜日
- 前日／翌日への移動
- 今日へ戻る
- 患者検索
- `＋電話予約`
- `＋窓口受付`（必要な場合）

### 時間枠表示

各時間枠を1行または1カードとして表示する。

```text
09:00  2/3名  残り1名  デモ太郎（Web）／デモ花子（電話）
09:30  3/3名  満員     山田／佐藤／田中
10:00  0/3名  空き3名  予約なし
```

各枠で表示する情報は次のとおり。

- 開始時刻
- 予約数／定員
- 残り人数
- 空き／残りわずか／満員／停止
- 患者名
- Web／電話／窓口
- 初診／再診
- 受付状態
- その枠へ電話予約を追加するボタン

### 受付状態

受付状態は次の流れに限定し、画面上で変更できるようにする。

```text
予約済 → 来院 → 待機 → 診察中 → 会計待ち → 完了
```

### 空き人数の計算

```text
残り人数 = 枠の定員 - 有効な予約数 - 有効な一時ホールド数
```

取消済み予約は使用数に含めない。

## 4.4 例外通知

「要対応」は完全削除せず、例外があるときだけ表示する。

対象例：

- メール送信失敗
- 重複患者の疑い
- カルテ未紐付け
- 同日直前の変更・取消
- 予約枠の競合
- 折り返しが必要な問い合わせ

0件のときは、カードも見出しも表示しない。

## 4.5 ナビゲーション

常時表示する主ナビゲーションを次の3項目へ整理する。

- 今日の受付
- 患者検索
- その他

`その他` の中へ次をまとめる。

- お知らせ管理
- FAQ管理
- お問い合わせ
- 通知履歴
- 通知設定
- 詳細な診療枠設定

電話予約は独立タブではなく、今日の受付画面内の主要ボタンにする。

旧 `予約依頼` 画面は新規導線から外す。既存データの確認が必要な移行期間だけ「過去の予約依頼」としてその他へ残し、削除はデータ移行確認後に判断する。

## 5. ビジュアル方針

### デザインテーマ

受付業務向けの「静かで高密度な医療ワークボード」とする。

- 装飾より即時判断を優先
- 白・淡いアイボリーを主背景にする
- 緑はブランドと主要操作に限定する
- 状態色は淡い背景と濃い文字の組み合わせにする
- タブレットで1画面に情報が収まる密度を確保する

### 推奨カラートークン

```text
背景             #F7F7F3
カード           #FFFFFF
本文             #1D2722
補助文字         #65726B
ブランド緑       #267257
薄いブランド緑   #E7F1EC
空き             #E8F4ED / #256847
残りわずか       #FFF3D6 / #8A5A00
満員             #F3ECE9 / #7A382D
停止             #EEF0EF / #59635E
```

濃い緑のベタ塗りは、`＋電話予約` や確定操作など最重要ボタンに限定する。

### アクセシビリティ

- 色だけで状態を区別しない
- `空き2名`、`満員` など文字でも明示する
- タッチ対象は最低44px
- キーボード操作とフォーカス表示を維持する
- 通常文字と背景はWCAG AA相当のコントラストを確認する

## 6. データモデルとAPI計画

## 6.1 原則

既存の即時予約モデルを再利用する。

- `Appointment`
- `AppointmentSlot`
- `Patient`
- `SlotHold`
- `AppointmentAccessToken`

旧 `Reservation` は即時予約の業務フローへ混ぜない。

## 6.2 追加候補

予約照合の監査と不正試行対策が必要な場合、次を追加する。

### `AppointmentLookupEvent`

- `id`
- `appointmentId`（成功時のみ、任意）
- `result`（success / not_found / rate_limited）
- `clientKeyHash`
- `createdAt`

生のIP、電話番号、生年月日、入力値は保存しない。

### 予約管理セッション

第一候補は署名済みHttpOnly Cookieとし、DBへセッション行を追加しない。強制失効が必要と判断した場合のみ、ハッシュ化した短期セッションテーブルを追加する。

## 6.3 API

### `POST /api/appointments/lookup`

入力：

- appointmentCode
- phone
- birthDate

処理：

1. 入力正規化
2. IP単位と予約番号ハッシュ単位のレート制限
3. 予約番号で候補を取得
4. 電話番号・生年月日を一定時間比較
5. 成功時に短時間Cookieを発行
6. 成功・失敗とも個人情報をログへ残さない

### `POST /api/appointments/cancel`

- 予約管理セッション必須
- 既存の取消締切を再利用
- 二重取消でも安全な冪等処理
- 確認画面を経由
- 取消後に枠を即時開放

### `POST /api/appointments/reschedule`

- 予約管理セッション必須
- 既存の枠ロックと空き数再確認を再利用
- 元の枠と新しい枠を同一トランザクションで更新

## 6.4 レート制限

`appointment-lookup` を追加する。

- IP単位：15分に5回
- 予約番号ハッシュ単位：15分に5回
- 制限超過時も予約の存在を推測できない共通エラー

現在のインメモリフォールバックはVercelの複数インスタンス間で共有されない。公開前に共有レート制限ストアを設定するか、同等の永続ストアへ切り替える。

## 7. デモデータ計画

固定日付のデモデータを廃止し、シード実行日を基準に生成する。

### 今日

- 予約6〜10件
- Web・電話・窓口を混在
- 満員枠を1〜2枠
- 残り1名の枠を1〜2枠
- 空き枠を複数
- 来院、待機、診察中、完了を混在

### 明日以降

- 3〜5件程度
- 変更・取消を試せる予約を1件

### 架空性

- 氏名に「デモ」を含める
- 電話番号は架空の予約済み範囲を使用
- メールは `.invalid` ドメインを使用
- 実患者情報を入力しない注意表示を維持する

## 8. CSP警告への対応

本番CSPは緩和しない。

優先案：デモに不要なVercel Preview Toolbar / Feedbackを無効化する。

代替案：Toolbarを利用する場合だけ、Preview環境のCSPへVercel Liveの必要最小限の送信元を追加する。本番環境には追加しない。

実装時はVercelの現行仕様を公式ドキュメントで確認し、許可するドメインを推測で追加しない。

## 9. 実装フェーズ

### Phase 1：受付ホームの再構成

- 旧集計カードを撤去
- 今日の全診療枠を取得
- 枠ごとの予約数、定員、残数を計算
- 時間枠タイムラインを実装
- 各枠に患者と受付状態を表示
- 空き枠から電話予約を開始できるようにする
- 例外は件数がある場合だけ表示

### Phase 2：ナビゲーション整理

- 主ナビを「今日の受付」「患者検索」「その他」へ削減
- 電話予約を受付ホーム内へ移動
- 診療枠管理を日付移動・詳細設定へ分割
- お知らせ、FAQ、問い合わせ、通知設定をその他へ集約
- 旧予約依頼を主導線から外す

### Phase 3：患者の予約セルフサービス

- 予約照合フォームを作成
- 予約番号・電話番号・生年月日の照合APIを作成
- 短時間の予約管理Cookieを実装
- 既存メールトークンをCookieへ交換
- 確認・日時変更・取消を同一画面へ集約
- 共通エラー、レート制限、監査を実装

### Phase 4：デザイン調整

- CSSカラートークンを更新
- 濃い緑の利用範囲を縮小
- 時間枠の状態色を追加
- タブレット表示を最優先で調整
- モバイル・デスクトップを確認
- コントラストとキーボード操作を確認

### Phase 5：デモ品質とCSP

- 相対日付のデモシードへ変更
- 今日の予約・満員・空きが見えるデータを投入
- Preview Toolbarを無効化、またはPreview限定CSPを設定
- コンソール警告を再確認

### Phase 6：段階公開

- デモ環境へ先行公開
- スマートフォンから予約
- 別PCの受付画面へ即時反映されることを確認
- 患者側でメールなしの予約確認・変更・取消を確認
- 本番データへ影響しないことを確認
- 病院担当者の確認後に本番へ反映

## 10. 主な変更対象ファイル

### 患者側

- `src/app/appointments/manage/page.tsx`
- `src/app/reserve/page.tsx`
- `src/app/reserve/complete/page.tsx`
- `src/components/site-header.tsx`
- `src/components/mobile-nav.tsx`
- `src/lib/booking.ts`
- `src/lib/rate-limit.ts`
- 新規 `src/app/api/appointments/lookup/route.ts`
- 新規 予約照合フォームコンポーネント

### 受付側

- `src/app/admin/(dashboard)/page.tsx`
- `src/app/admin/(dashboard)/layout.tsx`
- `src/app/admin/(dashboard)/schedule/page.tsx`
- `src/components/admin/phone-appointment-form.tsx`
- `src/components/admin/reception-status-select.tsx`
- 新規 時間枠タイムラインコンポーネント
- 新規 その他メニューコンポーネント

### デザイン・環境

- `src/app/globals.css`
- `next.config.ts`
- `prisma/demo-seed.ts`
- 必要に応じて `prisma/schema.prisma` と新規マイグレーション

## 11. 検証項目

### 患者側

- メールなしで予約を照合できる
- 誤った情報では予約の存在を推測できない
- 他人の予約を表示できない
- 取消後に枠の残数が増える
- 日時変更後に旧枠と新枠の人数が正しい
- 取消締切を過ぎた場合は安全に拒否される
- 連続試行が制限される

### 受付側

- 今日の全枠が時刻順に表示される
- 予約0件の枠も空き枠として表示される
- 定員3名に対する残数が正しい
- Web・電話・窓口予約が同じ枠に表示される
- 取消済み予約が使用数に含まれない
- 電話予約登録後に該当枠へ反映される
- 例外0件では要対応欄が表示されない
- タブレットで主要操作がスクロール最小限で完了する

### 環境

- Productionは `aoyama_clinic` のみを使用
- Previewは `aoyama_clinic_demo` のみを使用
- デモデータが本番へ混入しない
- CSP警告が解消される
- 型チェック、ビルド、主要APIテストが成功する

## 12. 完成条件

- 受付スタッフが5秒以内に次の空き時間を判断できる
- 電話予約を今日の受付画面から開始できる
- 患者がメールや電話を使わず予約確認・変更・取消を完了できる
- 通常時は不要な集計カードや空の要対応欄が表示されない
- 主ナビゲーションが3項目以内に整理されている
- デモを開いた直後に、満員・残りわずか・空きの違いが分かる
- 本番CSPと本番データの安全性を維持したままデモ環境で検証できる

## 13. 実装上の判断

- 旧 `Reservation` テーブルは最初から削除しない
- まず画面と導線から切り離し、既存データの有無を確認する
- 患者照合は予約番号だけにしない
- 電話番号・生年月日を含む入力値をログへ残さない
- 本番CSPをPreviewの都合で弱めない
- 色の変更は装飾ではなく、受付判断の速度を上げる状態設計として行う
