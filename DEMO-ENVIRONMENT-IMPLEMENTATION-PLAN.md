# 病院向けデモ環境 実装計画書

## 1. 目的

青山整形外科サイトを病院関係者に説明する際、空の画面ではなく、実際の受付業務をイメージできる状態で体験してもらう。

同時に、デモで入力された氏名・電話番号などの情報が本番環境や実患者データに混入しないようにする。

## 2. 基本方針

- 本番環境とデモ環境を完全に分離する
- デモ環境ではモックデータをあらかじめ用意する
- デモ中にスマートフォンから入力した予約を、別のPCの管理画面で確認できるようにする
- デモ環境に入力されたデータは、一定時間後または管理者操作で削除する
- 画面上に「デモ環境」「架空データ」と明示する
- 実在する患者情報を入力しないよう、入力画面とログイン画面で注意喚起する

## 3. 完成時の利用イメージ

```text
スマートフォン：デモ予約フォームへ入力
        ↓
デモ専用データベースへ一時保存
        ↓
PC：スタッフ入口から管理画面へログイン
        ↓
予約・患者・問い合わせ情報を確認
        ↓
デモ終了後に全データ削除、または自動削除
```

## 4. デモ環境の構成

### 4.1 環境の分離

- 本番URLとデモURLを分ける
- 本番SupabaseプロジェクトとデモSupabaseプロジェクトを分ける
- Vercelの環境変数も本番用・デモ用で分ける
- デモ環境から本番データベースへ接続できない構成にする

### 4.2 デモ表示

管理画面と予約画面に、次のような表示を追加する。

> デモ環境｜表示される患者情報・予約情報はすべて架空です。

患者向け画面には、次の注意を表示する。

> 実在する氏名・電話番号・メールアドレスは入力しないでください。

## 5. 用意するモックデータ

初回セットアップ時に、以下の架空データを登録する。

- 患者：5〜10名
- 本日の予約：5件程度
- 今週の予約：10〜15件
- 電話予約：2〜3件
- 予約キャンセル：1〜2件
- お問い合わせ：3件程度
- お知らせ：2件
- FAQ：5件程度
- 診療枠：曜日・時間帯ごとに設定

データは「デモ 太郎」「山田 花子」など明らかな架空名を使用し、実在しそうな個人情報は避ける。

## 6. デモで見せる操作シナリオ

病院への説明では、次の順番で操作できる状態にする。

1. 公開サイトのフッターから「スタッフ入口」へ移動
2. デモ用スタッフアカウントでログイン
3. 今日の予約一覧を確認
4. 患者情報を開く
5. スマートフォンから新しい予約を登録
6. PCの管理画面を更新して予約が表示されることを確認
7. 電話予約を登録
8. 予約枠を変更またはキャンセル
9. 問い合わせを確認
10. お知らせを公開・変更
11. デモデータを削除して初期状態へ戻す

## 7. データ保持と削除

### 必須機能

- デモデータに `demo` 識別子を付ける
- デモ環境のデータだけを削除する管理機能を用意する
- デモ予約の作成日時を記録する
- 作成から24時間以上経過したデータを自動削除する
- デモ終了時に全データを削除できるボタンを用意する
- 削除前に確認ダイアログを表示する

### 注意点

「データベースに一切保存しない」方式では、スマートフォンと別PCの間でデータを共有できない。そのため、デモ専用データベースには保存するが、本番データベースとは完全分離する方式を採用する。

## 8. セキュリティと個人情報対策

- デモ環境に実患者データをコピーしない
- 実在する個人情報の入力を禁止する
- デモ環境のデータベース権限を本番と分離する
- デモ用パスワードは本番用と異なるものにする
- デモ環境を検索エンジンに登録しない
- ログイン画面、予約画面、管理画面にデモ表示を出す
- デモ用URLを不特定多数へ公開しない
- デモ終了後にデータを削除する

## 9. 実装ステップ

### Phase 1：デモ環境の準備

- デモ用Supabaseプロジェクトを作成
- デモ用Vercel環境またはデモ用URLを作成
- デモ用環境変数を設定
- 本番環境とデモ環境の接続先を確認

### Phase 2：モックデータ投入

- Seedスクリプトを作成
- 患者、予約、問い合わせ、お知らせ、FAQを登録
- 初期データへ戻すリセット処理を作成

### Phase 3：デモ表示の追加

- 画面上部にデモ環境バナーを追加
- 予約フォームに架空データ利用の注意を追加
- 管理画面にデモ環境の表示を追加
- デモ用ログイン案内を整備

### Phase 4：自動削除

- 作成日時を利用した削除処理を作成
- 定期実行の仕組みを設定
- 全データ削除ボタンを管理画面に追加
- 削除結果を確認できるようにする

### Phase 5：動作確認

- スマートフォンで予約を入力
- PCで管理画面にログイン
- 入力した予約が表示されることを確認
- 本番データベースに保存されていないことを確認
- デモデータの削除を確認
- 未ログイン状態で管理画面へアクセスできないことを確認

## 10. 完成条件

- 病院担当者が10秒以内にスタッフ入口を見つけられる
- ログイン後すぐに予約・患者・問い合わせの例を確認できる
- スマートフォンから入力した予約をPCで確認できる
- デモデータが本番データベースに存在しない
- デモ終了後にデータを全削除できる
- デモ環境であることが常に分かる
- デモ環境の操作が本番環境へ影響しない

## 11. 推奨する完成状態

病院へ見せる直前に、デモ環境を初期化し、架空の患者・予約・問い合わせを表示した状態にする。

説明時は「スマートフォンで予約する患者側」と「PCで確認する受付側」を分けて操作し、予約が管理画面へ反映される流れを見せる。

デモ終了後は、全データ削除を実行してから環境を停止または非公開にする。
